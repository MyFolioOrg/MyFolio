services:
  postgres:
    image: postgres:16
    container_name: myfolio-auth
    restart: always
    environment:
      POSTGRES_USER: ${DATASOURCE_USERNAME}
      POSTGRES_PASSWORD: ${DATASOURCE_PASSWORD}
      POSTGRES_DB: auth
    ports:
      - "5433:5432"
